---
vmwaretools::udevrefresh_command: '/sbin/udevadm control --reload-rules && /sbin/udevadm trigger --action=add --subsystem-match=scsi'
vmwaretools::repopath: "/tools/releases/latest/rhel5/%{architecture}"
